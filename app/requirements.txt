# Step 2 — only Gradio so the install is fast enough for a smoke test loop.
# Step 3 will add:
#   klippbok[all]  # pulls torch, transformers, scenedetect, opencv, google-genai, pydantic, rich
#   pillow>=10.0   # used by the Manifest Reviewer thumbnail cache
gradio>=5.0,<6.0
