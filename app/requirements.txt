# UI
gradio>=5.0,<6.0
pillow>=10.0

# Klippbok itself — pulls torch, transformers, scenedetect, opencv-python,
# google-genai, pydantic, rich. The torch.js installer runs immediately after
# this (see install.js) to swap in a GPU-appropriate torch build when possible.
klippbok[all]
